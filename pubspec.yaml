name: fogosmobile
description: Fogos.pt new iOS and Android app

version: 5.0.1+500010

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.1.2
  flutter_test:
    sdk: flutter
  melos: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
    - assets/partners/